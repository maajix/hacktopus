aliases:
  subdomains:
    description: "Find subdomains of a given domain"
    command: "-silent -d {{url}}"
    variables:
      - name: url
        description: "Target domain"
        transform: url_to_domain
