aliases:
  scan:
    description: "Additional XSS scanning with different payloads"
    command: "--crawl --params -l 3 -u {{url}}"
    variables:
      - name: target
        description: "Target URL"